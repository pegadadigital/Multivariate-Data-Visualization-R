# Define a local data directory
# Set permissions for the container:
#   sudo chown -R 1000 ${LOCAL_WORKING_DIR}

LOCAL_WORKING_DIR=/d/Projetos/Multivariate-Data-Visualization-R/Notebooks

# Generate an access token like this
#   import IPython as IPython
#   hash = IPython.lib.passwd("S-E-C-R-E-T")
#   print(hash)
# You can use the script generate_token.py

ACCESS_TOKEN=sha1:97b844925f79:1bf38e6b9d099f0a6f41a1a74b8f6af66ece6bd8

# Host port
PORT=8888

# Provide data sets
LOCAL_DATASETS=/d/Projetos/Multivariate-Data-Visualization-R/Notebooks/datasets

# Provide local modules
LOCAL_MODULES=/d/Projetos/Multivariate-Data-Visualization-R/Notebooks/modules

# SSL
# Generate cert like this:
#   openssl req -x509 -nodes -newkey rsa:2048 -keyout jupyter.pem -out jupyter.pem
# Copy the jupyter.pem file into the location below.
LOCAL_SSL_CERTS=/d/Projetos/Multivariate-Data-Visualization-R/SSLCERT